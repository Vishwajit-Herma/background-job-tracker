apiVersion: apps/v1
kind: Deployment
metadata:
  name: background-job-tracker-redis
  labels:
    app: background-job-tracker
    component: redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: background-job-tracker
      component: redis
  template:
    metadata:
      labels:
        app: background-job-tracker
        component: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
              name: redis
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
          livenessProbe:
            exec:
              command:
                - redis-cli
                - ping
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - redis-cli
                - ping
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: background-job-tracker-redis-master
  labels:
    app: background-job-tracker
    component: redis
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
  selector:
    app: background-job-tracker
    component: redis
